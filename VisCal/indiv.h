#include "layers.h"



class indiv{
public:
	double score;
	void calScore(const calLayer& _calLayer,const trainData&){}

	vector<unsigned int[3]> size_preL;
	vector<unsigned int> size_postL;

	vector<vector<krnl>> krnls;
	vector<conn> conns;
	vector<thsd> thsds;
};